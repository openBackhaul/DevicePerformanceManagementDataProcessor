- get mountNameList from ds
- FOR EACH mountName in mountNameList:
  - get sortedBatchTimestamps
  - delete expired batches
  - again get sortedBatchTimestamps + count size OR getBatchCount
  - if (batchCount == 0)
    delete device from dataStore

-> interfaceMetadata automatisch mitgelöscht, nicht mehr separat
  