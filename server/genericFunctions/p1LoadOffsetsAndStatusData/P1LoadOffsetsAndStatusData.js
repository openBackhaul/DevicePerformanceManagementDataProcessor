const ERRORS = require('./ErrorsEnum');

const p1LoadOffsetsAndStatusData = (input) => {
  try {

  } catch (error) {
    return ERRORS.GENERAL_ERROR;
  }
}

module.exports = p1LoadOffsetsAndStatusData;
