# testing/p1CalculateUtilization/1.0.0/scenarios.yaml
#
# Spec sources:
# - spec/Functions/p1StreamPmData/p1ProcessDevice/p1CreateResultCc/p1IterateEcPmSlices/p1CalculateUtilization/1.0.0/interface.yaml
# - spec/Functions/p1StreamPmData/p1ProcessDevice/p1CreateResultCc/p1IterateEcPmSlices/p1CalculateUtilization/1.0.0/variables.yaml
#
#


module:
  functionUnderTest:
    modulePath: "../../../genericFunctions/p1CalculateUtilization/1.0.0/P1CalculateUtilization.js"
    exportName: ""

processingSteps: []
errorMapping: {}

scenarios:
  # -------------------------
  # input validation 
  # -------------------------

  - id: "errHistoricalPerformanceDataNotProvided"
    description: "Should return error when historical-performance-data is missing"
    inputFixture: "input.json"
    expected:
      type: "error"
      errorEnum: "historicalPerformanceData not provided"
    mocks: []

  - id: "errHistoricalPerformanceDataInvalid"
    description: "Should return error when historical-performance-data is not an object"
    inputFixture: "input.json"
    expected:
      type: "error"
      errorEnum: "historicalPerformanceData invalid"
    mocks: []

  - id: "errAggregationGroupNotProvided"
    description: "Should return error when aggregation-group is missing"
    inputFixture: "input.json"
    expected:
      type: "error"
      errorEnum: "aggregationGroup not provided"
    mocks: []

  - id: "errAggregationGroupInvalid"
    description: "Should return error when aggregation-group is not an object"
    inputFixture: "input.json"
    expected:
      type: "error"
      errorEnum: "aggregationGroup invalid"
    mocks: []

  - id: "errResultCcNotProvided"
    description: "Should return error when result-cc is missing"
    inputFixture: "input.json"
    expected:
      type: "error"
      errorEnum: "result-cc not provided"
    mocks: []

  - id: "errResultCcInvalid"
    description: "Should return error when result-cc is not an object"
    inputFixture: "input.json"
    expected:
      type: "error"
      errorEnum: "result-cc invalid"
    mocks: []

  - id: "errMissingInput"
    description: "Should return General processing error when input is null"
    inputFixture: "input.json"
    expected:
      type: "error"
      errorEnum: "General processing error"
    mocks: []

  # -------------------------
  # granularity-period handling (variables.yaml)
  # -------------------------
  # "IF NOT PERIOD-15-MIN THEN terminate without change or error message"
  # => expect exact same historical-performance-data returned unchanged.

  - id: "okGranularityPeriodNot15Min_24Hours"
    description: "Should terminate without change when granularity-period is PERIOD-24-HOURS"
    inputFixture: "input.json"
    expected:
      type: "success"
      outputFixture: "output.json"
    mocks: []

  - id: "okGranularityPeriodNot15Min_UNKNOWN"
    description: "Should terminate without change when granularity-period is PERIOD-UNKNOWN"
    inputFixture: "input.json"
    expected:
      type: "success"
      outputFixture: "output.json"
    mocks: []

  - id: "okGranularityPeriodNot15Min_NOT_YET_DEFINED"
    description: "Should terminate without change when granularity-period is PERIOD-NOT_YET_DEFINED"
    inputFixture: "input.json"
    expected:
      type: "success"
      outputFixture: "output.json"
    mocks: []

  # -------------------------
  # success path
  # -------------------------

  - id: "okUtilizationAdded"
    description: "Should add utilization (and required intermediate fields) for PERIOD-15-MIN slices"
    inputFixture: "input.json"
    expected:
      type: "success"
      outputFixture: "output.json"
    mocks: []

  # -------------------------
  # internal computation failure 
  # -------------------------


  - id: "errUtilizationCouldNotBeAdded"
    description: "Should return error when utilization cannot be computed for PERIOD-15-MIN slice"
    inputFixture: "input.json"
    expected:
      type: "error"
      errorEnum: "Utilization could not be added"
    mocks: []